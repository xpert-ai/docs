---
title: '全局设置'
description: 'Mintlify 通过 docs.json 文件为你提供对文档外观与体验的完整控制'
icon: 'gear'
---

每个 Mintlify 站点都需要一个 `docs.json` 文件来配置核心设置。下面查看 [属性](#properties) 的详细说明。

## 属性

<ResponseField name="name" type="string" required>
项目名称，用于全局标题。

示例：`mintlify`

</ResponseField>

<ResponseField name="navigation" type="Navigation[]" required>
  由分组与分组内页面组成的数组
  <Expandable title="Navigation">
    <ResponseField name="group" type="string">
    分组名称。

    示例：`Settings`

    </ResponseField>
    <ResponseField name="pages" type="string[]">
    将作为页面的 Markdown 文件相对路径。

    示例：`["customization", "page"]`

    </ResponseField>

  </Expandable>
</ResponseField>

<ResponseField name="logo" type="string or object">
  Logo 图片路径，或包含 "light" 与 "dark" 两种模式路径的对象
  <Expandable title="Logo">
    <ResponseField name="light" type="string">
      浅色模式下的 logo 路径
    </ResponseField>
    <ResponseField name="dark" type="string">
      深色模式下的 logo 路径
    </ResponseField>
    <ResponseField name="href" type="string" default="/">
      点击 logo 的跳转地址
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="favicon" type="string">
  favicon 图片路径
</ResponseField>

<ResponseField name="colors" type="Colors">
  主题的十六进制颜色配置
  <Expandable title="Colors">
    <ResponseField name="primary" type="string" required>
      主色。用于高亮内容、分节标题与强调样式（浅色模式）
    </ResponseField>
    <ResponseField name="light" type="string">
      深色模式下的主色。用于高亮内容、分节标题与强调样式
    </ResponseField>
    <ResponseField name="dark" type="string">
      重要按钮的主色
    </ResponseField>
    <ResponseField name="background" type="object">
      浅色与深色模式下的背景色
      <Expandable title="Object">
        <ResponseField name="light" type="string" required>
          浅色模式下的背景色（十六进制）
        </ResponseField>
        <ResponseField name="dark" type="string" required>
          深色模式下的背景色（十六进制）
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="topbarLinks" type="TopbarLink[]">
  顶部导航栏中展示的链接数组（`name` 与 `url`）
  <Expandable title="TopbarLink">
    <ResponseField name="name" type="string">
    按钮名称。

    示例：`Contact us`
    </ResponseField>
    <ResponseField name="url" type="string">
    点击按钮后的链接。例如：`https://mintlify.com/docs`
    </ResponseField>

  </Expandable>
</ResponseField>

<ResponseField name="topbarCtaButton" type="Call to Action">
  <Expandable title="Topbar Call to Action">
    <ResponseField name="type" type={'"link" or "github"'} default="link">
    `link` 会显示按钮，`github` 会显示提供的仓库信息（包含 Star 数）。
    </ResponseField>
    <ResponseField name="url" type="string">
    如果是 `link`：按钮链接地址。
    
    如果是 `github`：加载 GitHub 信息的仓库地址。
    </ResponseField>
    <ResponseField name="name" type="string">
    按钮文本。只有当 `type` 为 `link` 时必填。
    </ResponseField>

  </Expandable>
</ResponseField>

<ResponseField name="versions" type="string[]">
  版本名称数组。仅当你希望在导航栏提供版本下拉时使用。
</ResponseField>

<ResponseField name="anchors" type="Anchor[]">
  锚点数组，包含 `icon`、`color` 和 `url`。
  <Expandable title="Anchor">
    <ResponseField name="icon" type="string">
    用于展示锚点的 [Font Awesome](https://fontawesome.com/search?q=heart) 图标。

    示例：`comments`
    </ResponseField>
    <ResponseField name="name" type="string">
    锚点名称。

    示例：`Community`
    </ResponseField>
    <ResponseField name="url" type="string">
      URL 起始路径，用于标记哪些页面属于该锚点。通常是你放置页面的文件夹名称。
    </ResponseField>
    <ResponseField name="color" type="string">
      锚点图标背景色（十六进制）。也可传入包含 `from` 与 `to` 的对象作为渐变色。
    </ResponseField>
    <ResponseField name="version" type="string">
      若你希望仅在选择正确版本时显示该锚点，可使用该字段。
    </ResponseField>
    <ResponseField name="isDefaultHidden" type="boolean" default="false">
      若为 `true`，则默认隐藏该锚点，只有用户直接访问其内部页面时才会显示。
    </ResponseField>
    <ResponseField name="iconType" default="duotone" type="string">
      可选值："brands"、"duotone"、"light"、"sharp-solid"、"solid" 或 "thin"
    </ResponseField>

  </Expandable>
</ResponseField>

<ResponseField name="topAnchor" type="Object">
  覆盖最顶部锚点的默认配置。
  <Expandable title="Object">
    <ResponseField name="name" default="Documentation" type="string">
      最顶部锚点名称
    </ResponseField>
    <ResponseField name="icon" default="book-open" type="string">
      Font Awesome 图标。
    </ResponseField>
    <ResponseField name="iconType" default="duotone" type="string">
      可选值："brands"、"duotone"、"light"、"sharp-solid"、"solid" 或 "thin"
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="tabs" type="Tabs[]">
  导航标签数组。
  <Expandable title="Tabs">
    <ResponseField name="name" type="string">
      标签名称。
    </ResponseField>
    <ResponseField name="url" type="string">
      URL 起始路径，用于标记哪些页面属于该标签。通常是你放置页面的文件夹名称。
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="api" type="API">
  API 相关配置。了解更多 API 页面内容请参考 [API Components](/api-playground/demo)。
  <Expandable title="API">
    <ResponseField name="baseUrl" type="string">
      所有 API 端点的基础地址。如果 `baseUrl` 是数组，会启用多个可切换的基础地址。
    </ResponseField>

    <ResponseField name="auth" type="Auth">
      <Expandable title="Auth">
        <ResponseField name="method" type='"bearer" | "basic" | "key"'>
          所有 API 端点使用的认证策略。
        </ResponseField>
        <ResponseField name="name" type="string">
        API Playground 中使用的认证参数名称。

        若 method 为 `basic`，格式应为 `[usernameName]:[passwordName]`
        </ResponseField>
        <ResponseField name="inputPrefix" type="string">
        认证输入框的默认前缀值。

        例如 `inputPrefix` 为 `AuthKey` 时，输入框默认值会以 `AuthKey` 作为前缀。
        </ResponseField>
      </Expandable>
    </ResponseField>

    <ResponseField name="playground" type="Playground">
      API Playground 的配置

      <Expandable title="Playground">
        <ResponseField name="mode" default="show" type='"show" | "simple" | "hide"'>
          控制 Playground 的显示方式：展示、隐藏，或仅显示端点（`simple`）。

          了解更多请查看 [playground guides](/api-playground/demo)
        </ResponseField>
      </Expandable>
    </ResponseField>

    <ResponseField name="maintainOrder" type="boolean">
      开启后，OpenAPI 页面中的字段顺序将与 OpenAPI 文件中定义的顺序一致。

      <Warning>此行为将很快成为默认值，届时该字段会被弃用。</Warning>
    </ResponseField>

  </Expandable>
</ResponseField>

<ResponseField name="openapi" type="string | string[]">
  指向 OpenAPI 文件的 URL 或相对路径字符串，或字符串数组。
  
  示例：
  <CodeGroup>
    ```json Absolute
    "openapi": "https://example.com/openapi.json"
    ```
    ```json Relative
    "openapi": "/openapi.json"
    ```
    ```json Multiple
    "openapi": ["https://example.com/openapi1.json", "/openapi2.json", "/openapi3.json"]
    ```
  </CodeGroup>

</ResponseField>

<ResponseField name="footerSocials" type="FooterSocials">
  社交媒体账号对象，键为平台名，值为对应账号的 URL。
  
  示例：
  ```json
  {
    "x": "https://x.com/mintlify",
    "website": "https://mintlify.com"
  }
  ```
  <Expandable title="FooterSocials">
    <ResponseField name="[key]" type="string">
    取值之一：`website`、`facebook`、`x`、`discord`、`slack`、`github`、`linkedin`、`instagram`、`hacker-news`
    
    示例：`x`
    </ResponseField>
    <ResponseField name="property" type="string">
    社交平台链接。
    
    示例：`https://x.com/mintlify`
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="feedback" type="Feedback">
  启用反馈按钮的配置

  <Expandable title="Feedback">
    <ResponseField name="suggestEdit" type="boolean" default="false">
    启用“建议修改”按钮，允许用户通过 PR 提交修改建议。
    </ResponseField>
    <ResponseField name="raiseIssue" type="boolean" default="false">
    启用“提出问题”按钮，允许用户对文档提 issue。
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="modeToggle" type="ModeToggle">
  自定义深色模式切换。
  <Expandable title="ModeToggle">
    <ResponseField name="default" type={'"light" or "dark"'}>
      设置新用户默认模式为浅色或深色。未设置时，默认跟随系统设置。
    </ResponseField>
    <ResponseField name="isHidden" type="boolean" default="false">
      设为 true 可隐藏深/浅色切换。你可以将 `isHidden` 与 `default` 组合使用，强制文档仅使用浅色或深色。例如：
      
      <CodeGroup>
      ```json Only Dark Mode
      "modeToggle": {
        "default": "dark",
        "isHidden": true
      }
      ```

      ```json Only Light Mode
      "modeToggle": {
        "default": "light",
        "isHidden": true
      }
      ```
      </CodeGroup>

    </ResponseField>

  </Expandable>
</ResponseField>

<ResponseField name="backgroundImage" type="string">
  显示在每个页面背后的背景图。示例可查看
  [Infisical](https://infisical.com/docs) 与 [FRPC](https://frpc.io)。
</ResponseField>
